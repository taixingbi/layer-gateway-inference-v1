# Optional gateway environment (copy to `.env` in the gateway working directory if needed).

# IANA timezone for the `ts` field in JSON logs (default America/New_York; EST/EDT → US Eastern).
# LOG_TIMEZONE=UTC

# Deployment label for your own metrics or tooling (not added to log JSON by default).
# ENV=production
